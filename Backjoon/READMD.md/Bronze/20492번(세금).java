import java.math.BigInteger;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
    	Scanner scan = new Scanner(System.in);
    	
    	long A = scan.nextInt();
    	long B = Math.round(A * 0.8)+Math.round(Math.round(A * 0.2) * 0.78);
    	System.out.println(Math.round(A * 0.78)
    			+" "+ B);
    }
}
import java.math.BigInteger;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
    	Scanner scan = new Scanner(System.in);
    	
    	long A = scan.nextInt();
    	long B = Math.round(A * 0.8)+Math.round(Math.round(A * 0.2) * 0.78);
    	System.out.println(Math.round(A * 0.78)
    			+" "+ B);
    }
}
